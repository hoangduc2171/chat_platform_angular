
export type navigationItem = {
    id: number, 
    title: string,
    icon: string, 
    path: string,
}

export type User = {
    id: number, 
    fullname: string, 
    email: string,
    avatar: string
}
